@model IEnumerable<ExamenAlexanderFuela1.Controllers.ProductoController.ProductoTotal>

@{
    ViewData["Title"] = "MostrarProductosConCompras2";
}

<h1>MostrarProductosConCompras2</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VecesComprado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.ProductoId)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.NombreProducto)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.VecesComprado)
            </td>

        </tr>
}
    </tbody>
</table>
